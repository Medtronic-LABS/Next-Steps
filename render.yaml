services:
  - type: web
    name: openphc-cce-mock
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node cce-local-mock.cjs
    healthCheckPath: /v1/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
